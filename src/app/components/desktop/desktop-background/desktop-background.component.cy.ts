import { DesktopBackgroundComponent } from './desktop-background.component';

describe('DesktopBackgroundComponent', () => {
  it('should mount', () => {
    cy.mount(DesktopBackgroundComponent);
  });
});
